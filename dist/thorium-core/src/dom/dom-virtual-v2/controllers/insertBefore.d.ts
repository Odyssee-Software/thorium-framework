import { VirtualElement } from "../virtual-element";
/** Insère un élément virtuel avant un élément virtuel de référence au sein d'un élément virtuel parent */
export declare const insertBefore: (parent: VirtualElement, newChild: VirtualElement, refChild: VirtualElement) => void;
