import { VirtualElement } from "../virtual-element";
/** Ajoute un élément virtuel enfant à un élément virtuel parent */
export declare const appendChild: (parent: VirtualElement, child: VirtualElement) => void;
