import { VirtualElement } from "../virtual-element";
/**  Supprime un élément virtuel enfant d'un élément virtuel parent */
export declare const removeChild: (parent: VirtualElement, child: VirtualElement) => void;
