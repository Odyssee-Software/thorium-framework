import { VirtualElement } from '../virtual-element';
/** Remplace un élément virtuel enfant par un autre élément virtuel au sein d'un élément virtuel parent */
export declare const replaceChild: (parent: VirtualElement, newChild: VirtualElement, oldChild: VirtualElement) => void;
