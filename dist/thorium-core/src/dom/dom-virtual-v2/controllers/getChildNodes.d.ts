import { VirtualElement } from "../virtual-element";
/** Renvoie la liste des éléments virtuels enfants d'un élément virtuel donné */
export declare const getChildNodes: (ve: VirtualElement) => VirtualElement[];
