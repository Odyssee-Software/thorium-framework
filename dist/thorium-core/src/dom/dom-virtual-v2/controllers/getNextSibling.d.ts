import { VirtualElement } from "../virtual-element";
/** Renvoie l'élément virtuel suivant (le frère suivant) d'un élément virtuel donné */
export declare const getNextSibling: (ve: VirtualElement) => void;
