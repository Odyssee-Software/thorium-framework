import { VirtualElement } from "../virtual-element";
/** Définit la valeur d'un attribut pour un élément virtuel spécifié */
export declare const setAttribute: (ve: VirtualElement, attributeName: string, value: any) => void;
